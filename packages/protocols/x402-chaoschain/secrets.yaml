# ChaosChain x402 - Secrets Configuration
# This is a TEMPLATE file. DO NOT commit actual secrets.
# 
# For local development, secrets are loaded from .env file.
# For production, use a secrets manager (1Password, AWS Secrets Manager, etc.)
#
# See: https://docs.chain.link/cre/guides/managing-secrets

local-simulation:
  CRE_ETH_PRIVATE_KEY:
    type: env
    name: PRIVATE_KEY

staging:
  CRE_ETH_PRIVATE_KEY:
    type: env
    name: PRIVATE_KEY

production:
  CRE_ETH_PRIVATE_KEY:
    type: env
    name: PRIVATE_KEY

